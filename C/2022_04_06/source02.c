/*
#include <stdio.h>

int main(void) {
	// 정수형 변수: 4bye
	int a = 10, b = 20;
	int total = a + b;

	// 실수형 변수

	// Unity: float 주로 사용: 소수점 6자리 : 4byte
	float fa = 1.5f, fb = 3.7f;
	float total_f = fa + fb;

	// Visual Studio: double 주로 사용: 표현 범위 소수점 15자리 : 8byte
	double da = 5.5, db = 30.7;
	double total_d = da + db;

	// 정수형 변수 출력
	printf("%d + %d = %d\n",a,  b,  total);

	// 실수형 변수 출력
	printf("%f + %f = %f\n", fa, fb, total_f);
	printf("%f + %f = %f\n", da, db, total_d);


	return 0;
}

*/